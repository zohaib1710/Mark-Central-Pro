import { chromium } from 'playwright-core';
const browser=await chromium.launch({headless:true,executablePath:'C:/Program Files (x86)/Microsoft/Edge/Application/msedge.exe'});const width=Number(process.argv[3]||390);const file=process.argv[2]||'index.html';const page=await browser.newPage({viewport:{width,height:850}});await page.goto(`http://127.0.0.1:8000/${file}`);console.log(await page.evaluate(()=>{const overflow=[...document.querySelectorAll('body *')].map(e=>({e,r:e.getBoundingClientRect(),c:getComputedStyle(e)})).filter(x=>x.c.display!=='none'&&x.c.visibility!=='hidden'&&(x.r.right>innerWidth+1||x.r.left<-1)).map(x=>({tag:x.e.tagName,class:x.e.className,left:x.r.left,right:x.r.right,width:x.r.width,text:x.e.textContent.trim().slice(0,50)}));return {innerWidth,scrollWidth:document.documentElement.scrollWidth,bodyWidth:document.body.scrollWidth,overflow}}));await page.screenshot({path:'reports/debug-layout.png'});await browser.close();
